```bash
sudo apt update        # 更新套件清單 (安裝軟體前必做)
sudo apt upgrade       # 升級所有已安裝的套件
sudo apt install [pkg] # 安裝軟體 (如: sudo apt install git)
sudo apt remove [pkg]  # 移除軟體 (保留設定檔)
sudo apt purge [pkg]   # 移除軟體 (連同設定檔刪除)
sudo apt autoremove    # 自動清除不再需要的相依套件
```